# 第26章 画像生成プロンプト

## fig-ch26-schema-split.png

- 対応箇所: 第26章 章冒頭「この章を読む理由」
- 目的: 旧スキーマの横長な注文行を、ユーザー行と注文行へ分割するイメージを示す。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。左側に「旧スキーマ: orders_old」として、orderId, userId, userName, amount を含む横長のテーブルを置く。中央に「migrate / split」の矢印を置く。右側に「新スキーマ」として、users テーブル userId, userName と orders テーブル orderId, userId, amount の二つの小さなテーブルを配置する。userId の対応は細い線で示す。文字は後から差し替えやすい大きめのラベル枠にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch26-query-preservation-square.png

- 対応箇所: 第26章「クエリ結果が保存されるとは何か」
- 目的: queryNew (migrate db) = queryOld db という保存性を、可換図式として視覚化する。
- 画像生成プロンプト:
  日本語の技術書向けの白背景フラットベクター図。左上に「旧DB」、右上に「新DB」、左下に「旧クエリ結果」、右下に「新クエリ結果」を置く。左上から右上へ「migrate」、左上から左下へ「queryOld」、右上から右下へ「queryNew」、左下から右下へ「同じビュー型 / 結果一致」という矢印を引く。右下と左下が一致することをチェックマークで示す。図中の文字は少なめ、大きめのラベル領域にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch26-pullback-join.png

- 対応箇所: 第26章「Pullback 的な整合制約」
- 目的: join を、共通キー userId による整合条件つきの合流として理解させる。
- 画像生成プロンプト:
  白背景のフラットなベクター図。左に「UserRow」、右に「OrderRow」、下中央に「userId」を置く。UserRow から userId へ矢印、OrderRow から userId へ矢印を引く。上中央に「Consistent pair / join result」を置き、UserRow と OrderRow から上中央へ矢印を引く。二つの userId が一致するときだけ上中央の join result が作られることを、細いチェックマークや等号で示す。数式は最小限にし、ラベル差し替え可能な配置にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch26-lean-boundary.png

- 対応箇所: 第26章「この定理が保証していないこと」
- 目的: Lean で証明する小さな核と、実DBで別途確認する範囲を区別させる。
- 画像生成プロンプト:
  日本語技術書向けの白背景フラットベクター図。中央に小さな円または角丸矩形で「Leanモデル: query preservation」と置く。その周囲に「SQL方言」「NULL」「トランザクション」「移行バッチ」「性能」「本番データ品質」「監視・復旧」を放射状に配置する。中央は証明対象、周辺はテストやレビュー対象であることを分かるように、中央と周辺を薄い線で分ける。色数は少なめ、文字は後から差し替えやすい。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
