# 第30章 画像生成プロンプト

## fig-ch30-overview.png

- 対応箇所: 第30章 章冒頭
- 目的: 1件のマイグレーションを `List.map` によってリスト全体へ持ち上げる流れを示す
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。左に「UserV1 のリスト」、中央に「List.map migrate」、右に「UserV2 のリスト」を配置する。各リストは縦に並んだ3つ程度のカードで表し、カード同士が同じ順序で対応していることを矢印で示す。下部に「件数保存」「順序保存」「各要素に同じ変換」という小さなラベル領域を置く。文字は後から差し替えやすいように余白を広く取る。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch30-map-as-functor.png

- 対応箇所: 第30章「List.map と関手の見方」
- 目的: 関数 `f : A -> B` が `List.map f : List A -> List B` に持ち上がることを示す
- 画像生成プロンプト:
  日本語の技術書向けの白背景ベクター図。上段に小さな箱「A」と「B」を置き、AからBへ矢印 `f` を描く。下段に大きめの箱「List A」と「List B」を置き、List AからList Bへ矢印 `List.map f` を描く。上段から下段へは「List」というラベル付きの縦矢印で対応を示す。図全体として、型と関数の対応がリストの世界へ持ち上がることが分かる構図にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch30-composition-law.png

- 対応箇所: 第30章「二段階移行の合成保存」
- 目的: `map (g . f)` と `map g . map f` が同じ結果になることを、二段階移行として示す
- 画像生成プロンプト:
  白背景のフラットなベクター図。左に「List UserV1」、右上に「List UserV3」、中央に「List UserV2」を配置する。左から右上へ斜め矢印で「map (normalize . migrate)」を描く。左から中央へ矢印「map migrate」、中央から右上へ矢印「map normalize」を描く。二つの経路が同じ結果に到達することを、右上の箱へ合流する構図で表す。可換図式風だが、数学記号は少なめにする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch30-roundtrip-lift.png

- 対応箇所: 第30章「round-trip をリスト全体へ持ち上げる」
- 目的: 1件の `rollback (migrate u) = u` が、リスト全体の `rollbackAll (migrateAll xs) = xs` へ拡張されることを示す
- 画像生成プロンプト:
  日本語技術書向けの白背景ベクター図。上段に1件のカード「u : UserV1」から「migrate」を通って「UserV2」へ行き、「rollback」で元の「u」へ戻る丸い矢印を描く。下段には複数カードのリスト「xs : List UserV1」から「migrateAll」へ進み、「rollbackAll」で元のリストへ戻る大きな丸い矢印を描く。上段から下段へ「各要素で成り立つので、リスト全体でも成り立つ」という関係が見える構図にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
