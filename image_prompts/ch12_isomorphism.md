# 第12章 画像生成プロンプト

## fig-ch12-overview.png

- 対応箇所: 第12章 章冒頭
- 目的: 同型を、ソフトウェア上の相互変換とLean上のround-trip証明として対応づける。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。中央に「同型 = 情報を失わない相互変換」と大きく置く。左側に「ソフトウェア例」としてスキーマ移行、DTO変換、encoder / decoderの3つの小さなカードを置く。右側に「Leanでの表現」として、toFun、invFun、left_inv、right_invを持つ構造体風のカードを置く。下部に「round-trip law」として二方向の矢印を配置する。余白を広く取り、文字は後から差し替えやすいラベル枠として配置する。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch12-round-trip.png

- 対応箇所: 第12章 12.2節 encoder / decoder と round-trip
- 目的: encodeしてdecodeする向きと、decodeしてencodeする向きの二つが必要であることを示す。
- 画像生成プロンプト:
  白背景のフラットなベクター図。左に「値 A」、右に「表現 B」の大きな箱を置く。AからBへ上向きの曲線矢印で「encode / to」、BからAへ下向きの曲線矢印で「decode / from」を示す。A側には「decode(encode(a)) = a」、B側には「encode(decode(b)) = b」の短いラベル領域を置く。二つの閉じたループが見えるようにする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch12-bidirectional-not-iso.png

- 対応箇所: 第12章 12.3節 A → B と B → A だけでは同型ではない
- 目的: 双方向の関数が存在しても、情報を捨てるとround-tripが失敗することを視覚化する。
- 画像生成プロンプト:
  白背景のフラットなベクター図。左に「UserV1: id, email, displayName」というカード、右に「Unit: ()」という小さな点または小さい箱を配置する。左から右へ「eraseToUnit」の矢印、右から左へ「defaultUser」の矢印を描く。左のカードから右へ行く途中で email と displayName が薄く消えていく表現を入れる。下部に「戻しても元のUserV1にならない」という警告ラベル領域を置く。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch12-user-migration.png

- 対応箇所: 第12章 12.6節 UserV1 と UserV2 の lossless migration
- 目的: UserV1の平坦なフィールドがUserV2のネストした構造へ情報を失わず対応することを示す。
- 画像生成プロンプト:
  白背景のフラットなベクター図。左にUserV1のレコードカードを置き、フィールドとして id, email, displayName の3行を表示する。右にUserV2のレコードカードを置き、userId, contact.email, profile.displayName の3行を表示する。idからuserId、emailからcontact.email、displayNameからprofile.displayNameへ、それぞれ対応線を引く。上側にmigrateの矢印、下側にrollbackの矢印を置く。全体として情報が一対一に移る印象にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch12-migration-classification.png

- 対応箇所: 第12章 12.7節 実務上の注意
- 目的: 完全同型、片方向互換、制約付き同型、情報損失ありを区別する。
- 画像生成プロンプト:
  白背景のフラットなベクター図。横方向に4つのカードを並べる。カード1は「完全同型」で双方向矢印とチェック2つ。カード2は「片方向互換」で片方の矢印だけ太く、もう片方は点線。カード3は「制約付き同型」で双方向矢印の上に「well-formed条件」のフィルターを置く。カード4は「情報損失あり」で矢印途中に小さな欠けたピースや薄く消えるフィールドを置く。色数は抑え、分類が一目で分かるようにする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
