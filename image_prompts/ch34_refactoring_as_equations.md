# 第34章 画像生成プロンプト

## fig-ch34-refactoring-equation-map.png

- 対応箇所: 第34章 章冒頭
- 目的: リファクタリングを、変更前の処理と変更後の処理を同じ仕様へ結びつける等式として理解させる。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。左に「変更前の処理」、右に「変更後の処理」を大きな角丸ボックスで配置し、中央に「意味保存の等式」というラベル付きの太い矢印または等号を置く。下部に「関数抽出」「処理順序変更」「共通化」「wrapper削除」「Option.bindの整理」の5つの小さなカードを並べる。上部には「同じ入力なら同じ出力」という短い見出しを置く。数式は最小限にし、文字は後から差し替えやすい余白を確保する。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch34-order-change.png

- 対応箇所: 第34章 処理順序の変更は交換法則を要求する
- 目的: 二つの処理を入れ替えてよいのは、可換な場合だけであることを示す。
- 画像生成プロンプト:
  白背景のフラットなベクター図。左上に入力 x、右上に処理 f の後に処理 g を通る経路、左下に処理 g の後に処理 f を通る経路を置き、右側で同じ出力に合流する可換図式風の構成にする。中央に「g(f(x)) = f(g(x)) が必要」というラベルを置く。図の右端に小さな注意カードとして「足し算の手数料は交換できる」「double と addOne は交換できない」を対比して置く。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch34-option-bind-assoc.png

- 対応箇所: 第34章 モナド則による effectful code の整理
- 目的: `Option.bind` のネストした形と平坦化した形が、成功・失敗の流れとして同じ意味を持つことを示す。
- 画像生成プロンプト:
  日本語の技術書向けの白背景フラット図。左側に「変更前: ネストした bind」として、parsePositive から checkEven へ、さらに enrich へ進む入れ子の流れを描く。右側に「変更後: 先に合成してから bind」として、parsePositive と checkEven を一つの検査列にまとめ、その後 enrich へ進む流れを描く。各ステップの下に「noneなら停止」「someなら次へ」という小さな注記を置く。中央には「括弧を変えても結果は同じ」と示す等号を配置する。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
