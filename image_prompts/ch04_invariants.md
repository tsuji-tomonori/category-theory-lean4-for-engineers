# 第4章 画像生成プロンプト

## fig-ch04-invariant-flow.png

- 対応箇所: 第4章「この章を読む理由」
- 目的: データ構造、述語としての不変条件、操作、証明の関係を読者に把握させる。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。左に「データ構造 structure」、中央に「不変条件 predicate」、右に「操作 deposit / withdraw」、さらに右に「証明 theorem」を横方向の流れとして配置する。各要素を角丸長方形で示し、矢印で「値を作る」「条件を述べる」「操作する」「保存を証明する」という流れを表す。文字は後から差し替えやすいように大きめのラベル領域だけを置く。数式は最小限にし、Leanコード断片は入れない。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch04-pre-post-invariant.png

- 対応箇所: 第4章「事前条件・事後条件」
- 目的: 事前条件、操作、事後条件、不変条件の時間的な位置づけを説明する。
- 画像生成プロンプト:
  白背景のフラットなタイムライン図。左から右へ「操作前」「withdraw」「操作後」の3段階を配置する。操作前の上に「事前条件: amount <= balance」、操作後の上に「事後条件: new.balance + amount = old.balance」、タイムライン全体を囲む帯として「不変条件: 有効なAccountであり続ける」を示す。数式は短く、記号は大きめ。全体はシンプルな業務仕様レビュー用の図にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch04-subtype-gate.png

- 対応箇所: 第4章「Subtype による『条件を満たす値』」
- 目的: Subtypeを、値と証明を一緒に持つ「条件付きデータの入口」として理解させる。
- 画像生成プロンプト:
  白背景のフラットなベクター図。左に「通常のAccount値」が複数あり、中央にゲート状の枠「AccountValidチェック」、右に「ValidAccount = 値 + 証明」を配置する。ゲートを通過できる値と通過できない値を分ける。右側では箱の中に小さな2段構成で「a.val」と「a.property」を示す。Leanコードは入れず、概念図として分かりやすくする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
