# 第22章 関係・前順序・Galois 接続：画像生成プロンプト

## fig-ch22-equality-vs-order.png

- 対応箇所: 第22章 22.1「等式では足りない場面」
- 目的: 等式で扱う「完全に同じ」と、順序や関係で扱う「安全な近似」の違いを理解させる。
- 画像生成プロンプト:
  - 左側に「等式」の図。二つの同じ形の箱が双方向矢印で結ばれ、「変更前」「変更後」「同じ結果」とラベルがある。
  - 右側に「順序・近似」の図。大きな詳細情報の箱から、小さな公開情報の箱へ片方向矢印があり、落とされた情報は薄いグレーで示す。
  - 中央上に「同じでなくても、安全と言える場合がある」という見出し。
  - 数式は最小限にし、必要なら左に「=」、右に「≤ / 関係」とだけ置く。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch22-preorder-chain.png

- 対応箇所: 第22章 22.3「前順序：反射性と推移性を持つ比較」
- 目的: `public <= internal <= secret` というログ詳細度の前順序を視覚化する。
- 画像生成プロンプト:
  - 横方向に三つのノードを並べる。左から「public」「internal」「secret」。
  - ノード間に右向き矢印を置き、「情報量が増える」「公開可能性は下がる」という補助ラベルを添える。
  - 各ノードの下に短い説明を配置する。public は「公開可能」、internal は「内部用」、secret は「秘密情報あり」。
  - 反射性を示すため、各ノードに小さなループ矢印を置く。
  - 推移性を示すため、public から secret への薄い長い矢印を背景に置く。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch22-abstraction-concretization.png

- 対応箇所: 第22章 22.4「抽象化と具体化」
- 目的: 詳細な世界 `Detail` と抽象的な世界 `PublicView` を、抽象化 `erase` と具体化 `allow` で結ぶ構造を理解させる。
- 画像生成プロンプト:
  - 左側に縦の階層として「Detail」を描く。下から public、internal、secret の三段階。
  - 右側に縦の階層として「PublicView」を描く。下から publicOnly、mayContainPrivate の二段階。
  - 左から右へ `erase` の矢印を描く。public は publicOnly へ、internal と secret は mayContainPrivate へ向かう。
  - 右から左へ `allow` の矢印を描く。publicOnly は public へ、mayContainPrivate は secret へ向かう。
  - 「情報を落とす」「許される最大具体値を見る」という短い注釈を置く。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch22-galois-law.png

- 対応箇所: 第22章 22.5「Galois 接続：抽象化と具体化の対応」
- 目的: Galois 接続の同値 `alpha(c) <= a` iff `c <= gamma(a)` を、二つの世界の比較として直感的に示す。
- 画像生成プロンプト:
  - 左側に「具体側 C」、右側に「抽象側 A」の二つの領域を描く。
  - 具体側に点 c と gamma(a)、抽象側に alpha(c) と a を置く。
  - 具体側では c から gamma(a) へ「≤」の矢印を描く。
  - 抽象側では alpha(c) から a へ「≤」の矢印を描く。
  - 具体側から抽象側へ `alpha` の矢印、抽象側から具体側へ `gamma` の矢印を描く。
  - 中央に「この二つの比較が同値」と表示する。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch22-public-log-mask.png

- 対応箇所: 第22章 22.7「サンプル：詳細ログから公開ログへの安全な抽象化」
- 目的: 詳細ログから公開ログを作るとき、保存すべき関係と落とすべき情報を分けて見る設計レビューの流れを示す。
- 画像生成プロンプト:
  - 左に詳細ログのカードを描く。項目は userId、endpoint、token、detailLevel。
  - 右に公開ログのカードを描く。項目は endpoint のみ、または endpoint と publicView。
  - 左から右への太い矢印に「maskLog」と書く。
  - endpoint だけは同じ色で接続線を引き、「保存する関係」とラベルを付ける。
  - userId と token は薄いグレーにして、「落とす情報」とラベルを付ける。
  - 下部に「同型ではなく、安全な抽象化」と表示する。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
