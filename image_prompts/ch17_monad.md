# 第17章 画像生成プロンプト

## fig-ch17-overview.png

- 対応箇所: 第17章 章冒頭
- 目的: モナドを「文脈つき処理の合成」として捉える章全体の地図を示す。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。中央に「モナド = 文脈つき処理の合成」という大きな概念ボックスを置く。左側に具体例として「Option: 失敗するかもしれない」「Except: エラー理由を持つ」「State: 状態を渡す」「Async: 後で値が得られる」を縦に並べる。右側に Lean での表現として「pure」「bind」「モナド則」を配置する。下部に実務応用として「バリデーション」「失敗するマイグレーション」「状態更新」「非同期 API 呼び出し」を並べる。矢印は左から中央、中央から右、中央から下へ向ける。文字は後から差し替えやすいように、ラベル領域を広めに確保する。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch17-bind-flow.png

- 対応箇所: 第17章 17.2節「pure と bind」
- 目的: `bind` が成功時だけ次の処理へ進み、失敗時は停止することを視覚化する。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。左に「Option A」という入力ボックスを置き、そこから二股に分岐する。上の分岐は「some a」から「a を取り出す」へ進み、さらに「f a : Option B」へ進む。下の分岐は「none」から「次の処理を実行しない」へ進み、最終的に「none」へ到達する。全体として、成功ルートと失敗ルートが対比できる構図にする。色は成功ルートと失敗ルートを控えめに分ける。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch17-monad-laws.png

- 対応箇所: 第17章 17.3節「モナド則をリファクタリング規則として読む」
- 目的: 左単位律、右単位律、結合律を、コード整理の規則として読ませる。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。横に3つのカードを並べる。1枚目は「左単位律」で、`pure a >>= f` から `f a` へ矢印で簡略化する。2枚目は「右単位律」で、`x >>= pure` から `x` へ簡略化する。3枚目は「結合律」で、`(x >>= f) >>= g` と `x >>= (fun a => f a >>= g)` が同じ処理列を表すことを、括弧の位置だけが変わる図として示す。各カードの下に「不要な包みを消す」「最後の包み直しを消す」「括弧を整理する」という短い説明領域を置く。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch17-migration-pipeline.png

- 対応箇所: 第17章 17.5節「サンプル：失敗するマイグレーションを安全に合成する」
- 目的: 欠損を含む旧ユーザーデータが、`requireEmail` と `requireAge` を通って、成功または失敗へ分岐する流れを示す。
- 画像生成プロンプト:
  日本語の技術書に入れる16:9横長、白背景のフラットなベクター図。成功ルートを左から「UserDraft」「requireEmail」「UserWithEmail」「requireAge」「some UserV2」の順に矢印でつなぐ。「requireEmail」の下に「UserDraft → Option UserWithEmail」、「requireAge」の下に「UserWithEmail → Option UserV2」と小さく置く。各処理から下向きの失敗分岐を描き、前者は「メール欠損 → none」、後者は「年齢欠損 → none」へ到達させる。上部に「bind で成功時だけ次へ」と置く。文字列、特に requireEmail と requireAge は指定どおり正確に描き、章番号、図番号、図タイトルは画像内に入れない。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
