# 第15章 画像生成プロンプト

## fig-ch15-overview.png

- 対応箇所: 第15章「この章を読む理由」
- 目的: 関数外延性を、実装の違いではなく全入力での振る舞い一致として理解させる。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。中央に「関数外延性」と大きめのラベル領域を置く。左側に「旧実装 f」と「新実装 g」の二つの箱を置き、それぞれ異なる内部配線の抽象図にする。上から複数の入力 x1, x2, x3 が両方の箱に入り、右側で同じ出力 y1, y2, y3 に合流する構成にする。下部に「すべての入力で同じ出力なら、外から見た振る舞いは同じ」という説明ラベル領域を置く。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch15-funext-proof.png

- 対応箇所: 第15章「Lean 4 で点ごとの一致を書く」
- 目的: `funext` が関数等式 `f = g` を任意入力での等式 `f x = g x` へ分解する様子を示す。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。上段に大きなゴール箱「f = g」を置く。中央に下向き矢印を置き、矢印ラベル領域に「funext x」と書けるようにする。下段に複数の小さなゴール箱「f x = g x」「f x1 = g x1」「f x2 = g x2」を並べ、関数全体の等式が任意入力ごとの等式へ分解される構造にする。Lean の証明ステップを視覚化するが、コードは最小限にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch15-api-wrapper.png

- 対応箇所: 第15章「ミニケース：新旧 API client wrapper が同じ振る舞いをすること」
- 目的: 新旧 API client wrapper の同値性を、任意のリクエストでレスポンスが一致する図として説明する。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。左に「Request r」の箱を置き、そこから上下二本の矢印を出す。上段は「clientV1」、下段は「clientV2 または wrapper 経由」とする。右側で二つの矢印が同じ「Response」の箱へ合流する。中央または下部に「∀ r, clientV1 r = clientV2 r」というラベル領域を置く。API、wrapper、adapter の実務感が出るように、箱と矢印だけで構成する。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
