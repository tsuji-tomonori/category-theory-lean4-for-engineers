# 第39章 画像生成プロンプト

## fig-ch39-bridge-map.png

- 対応箇所: 第39章 章冒頭
- 目的: トポス、型理論、仕様言語、Leanがどのように接続されるかを、章全体の概念地図として示す。
- 画像生成プロンプト:
  日本語の技術書に入れる、白背景のフラットなベクター図。中央に「世界の中で論理を語る」という大きなラベル領域を置く。左側に「圏論」「対象」「射」「構造」、右側に「Lean」「Type」「Prop」「証明」、下側に「仕様DSL」「意味論」「モデル検査」「形式手法」を配置する。各領域を細い線で接続し、トポスが圏論と論理をつなぐ背景であることを示す。余白を広く取り、文字は後から差し替えやすい大きなラベル領域にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch39-internal-language.png

- 対応箇所: 第39章「内部言語という考え方」
- 目的: 外部から見たシステム全体と、各文脈の内部で使える論理が異なることを示す。
- 画像生成プロンプト:
  日本語の技術書向けの白背景ベクター図。外側に大きな枠として「外部から見たシステム全体」を置く。その中に、複数の小さな枠として「テナントA」「テナントB」「検証モデル」「本番環境」などの文脈を配置する。各小枠の中に「この文脈での状態」「この文脈での仕様」「この文脈での真偽」という3つのラベル領域を置く。外側から内側へ矢印を引き、「内部言語」というラベルを中央に配置する。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch39-spec-dsl-loop.png

- 対応箇所: 第39章「仕様言語・DSL・モデル検査との距離感」
- 目的: 仕様DSLの構文が意味論によって状態空間上の命題へ解釈され、Leanの証明やモデル検査へ接続される流れを示す。
- 画像生成プロンプト:
  日本語の技術書向けの白背景ベクター図。左に「DSLの構文」、中央に「意味論」、右に「状態空間上の命題」を配置する。右側から上下に分岐し、上に「Leanで証明」、下に「モデル検査で探索」を置く。最後に「設計レビューへ戻る」という矢印を左側へ戻し、仕様改善のループを表す。矢印は太すぎず、全体をすっきり整理する。文字は後から差し替えやすいように大きめのラベル領域にする。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
