# 第31章 画像生成プロンプト

## fig-ch31-free-forgetful-overview.png

- 対応箇所: 第31章 章冒頭・31.1
- 目的: 「自由に作る」操作と「構造を忘れる」操作が向かい合う関係であることを直感的に示す。
- 画像生成プロンプト:
  日本語の技術書に入れる白背景のフラットなベクター図。左側に「型 A」または「材料」を置き、中央上に「自由構成 F: A → List A」、右側に「モノイド List A」を置く。右から左へ戻る下向きまたは下側の矢印として「忘却 U: 構造を忘れて型だけを見る」を配置する。下部に「作る」と「忘れる」が一対であることを示す薄い帯を置く。数式は最小限にし、ラベルは大きめの差し替え可能なテキスト領域として配置する。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch31-free-monoid-list.png

- 対応箇所: 第31章 31.2 free monoid と list
- 目的: 要素ごとの関数 `A -> M` が、リスト全体のモノイド準同型 `List A -> M` を一意に決めることを示す。
- 画像生成プロンプト:
  白背景のフラットなベクター図。左上に「要素 a : A」の小さな点を複数配置し、それぞれが右上の「M の値」へ矢印で写る様子を描く。左下に `[a1, a2, a3]` のようなリストを箱で描き、右下に `f a1 ⊗ f a2 ⊗ f a3` のような集約結果の箱を描く。中央に `foldMap` と書いた太い矢印を配置する。下部に「空リスト → 単位元」「連結 → 演算」という二つの小さな注記を置く。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること

## fig-ch31-galois-adjunction.png

- 対応箇所: 第31章 31.4 Galois 接続との関係
- 目的: Galois 接続が前順序における随伴として読めることを示す。
- 画像生成プロンプト:
  日本語技術書向けの白背景ベクター図。左に「具体側 A の前順序」、右に「抽象側 B の前順序」を縦方向の段階リストとして描く。左から右へ `lower` または `α` の矢印、右から左へ `upper` または `γ` の矢印を描く。中央に `α(a) ≤ b  ⇔  a ≤ γ(b)` の式を大きく配置する。下に「比較可能性を別の世界へ翻訳する」と短い説明を置く。
- スタイル指定:
  - 日本語技術書向け
  - フラットなベクター図
  - 白背景
  - 余白多め
  - 色数は少なめ
  - 数式は最小限
  - 文字は後から差し替えやすい配置
- 避けること:
  - 写実的な人物
  - 複雑すぎる背景
  - 小さすぎる文字
  - 原典PDFの図に似せすぎること
