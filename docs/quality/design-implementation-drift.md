# 設計意図と実装のドリフト

| 設計意図・旧状態 | 現行実装 | 採用方針 | 移行コスト・状態 |
|---|---|---|---|
| 旧履歴では第0〜34章 | `main.tex` は第0〜40章＋付録A〜D | `main.tex` を実装上の正とする | 旧番号参照を自動参照へ移行、M |
| 大きなLeanファイル | `lean/<chapter>/codeNN.lean` 260件 | 現行の自己完結スニペット規約を採用 | 出典コメントとnamespaceの更新、M |
| コード直後に十分な説明 | 従来checkerは直後のproseだけを確認 | 目的・型・要点・限界を監査項目にする | 人手監査とchecker拡張、L |
| 用語・記号は使用前に説明 | 新出記法欄は主に第1〜10章 | 全章の依存mapと用語台帳で管理 | M |
| 重要主張に出典 | 本文引用がなくbibliographyが空 | claim ledgerから引用を追加 | L |
| 図は16:9 | 付録B/Dに比率例外 | 本文図は16:9、付録は可読性を根拠に例外可 | 例外を図版監査で記録、S |
| Part序文と章本文を分業 | Part 2/7に章要約との重複 | Part序文は課題・地図・読書ルートに限定 | 本文確定後に再監査、M |
| 旧構成案・執筆prompt・序文集と比較 | 指定4資料はrepo・提供添付に存在しない | 内容を推測しない | 資料入手まで `blocked` |

## 判断規則

現行ファイルを実装の事実、旧資料を設計意図として扱う。相違を無断で片側へ寄せず、採否、根拠、移行コストをこの文書に追記する。
