% Encoding / Japanese
\usepackage{cmap}
\usepackage{luatexja}
\usepackage{luatexja-fontspec}

% Math
\usepackage{amsmath,amssymb,mathtools}
\usepackage{newunicodechar}
% Lean/Mathlib symbols are rendered through math fonts so that the PDF does
% not depend on incomplete monospace Unicode coverage.
\newunicodechar{⟨}{\ensuremath{\langle}}
\newunicodechar{⟩}{\ensuremath{\rangle}}
\newunicodechar{⊢}{\ensuremath{\vdash}}
\newunicodechar{⟶}{\ensuremath{\longrightarrow}}
\newunicodechar{⥤}{\ensuremath{\rightsquigarrow}}
\newunicodechar{𝟙}{\ensuremath{\mathbb{1}}}

% Layout
\usepackage{geometry}
\geometry{margin=25mm}
\usepackage{setspace}
% Long identifiers and inline code occur frequently in this technical text.
% Give TeX enough emergency flexibility to avoid text protruding into margins.
\setlength{\emergencystretch}{3em}
\setstretch{1.12}
\usepackage{fancyhdr}
\setlength{\footskip}{12pt}
\usepackage{enumitem}
\setlist{nosep,leftmargin=2em}
\usepackage{booktabs,longtable,array}

% Figures
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc,fit,shapes.geometric}

% Boxes
\usepackage[most]{tcolorbox}
\tcbuselibrary{breakable,skins}

% Code
\usepackage{accsupp}
\usepackage[cachedir=_minted-main-lean-keywords]{minted}
\ifcsname MintedPygmentize\endcsname
  \renewcommand{\MintedPygmentize}{./tools/pygmentize_lean_keywords.py}
\fi
\usemintedstyle{friendly}
\newcommand{\copyablespace}{%
  \BeginAccSupp{method=escape,ActualText={\space}}%
  \phantom{\textvisiblespace}%
  \EndAccSupp{}%
}
\setminted[lean4]{
  fontsize=\small,
  breaklines=true,
  breakanywhere=true,
  breaksymbolleft={},
  frame=single,
  framerule=0.3pt,
  framesep=0.6em,
  rulecolor=\color{gray!40},
  autogobble=false,
  obeytabs=true,
  tabsize=2,
  showspaces=true,
  space=\copyablespace
}
% Hyperlinks
\usepackage[
  hidelinks,
  pdftitle={ソフトウェアエンジニアのための圏論と Lean 4 入門},
  pdfauthor={tsuji-tomonori},
  pdfsubject={圏論と Lean 4 による仕様・同値性・安全なマイグレーション},
  pdfkeywords={category theory, Lean 4, formal verification, software engineering}
]{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}

% Table of contents
\setcounter{tocdepth}{1}
\makeatletter
\renewcommand*{\l@chapter}[2]{%
  \ifnum \c@tocdepth >\m@ne
    \addpenalty{-\@highpenalty}%
    \addvspace{1.0em \@plus\jsc@mpt}%
    \begingroup
      \parindent\z@
      \rightskip\@tocrmarg
      \parfillskip-\rightskip
      \leavevmode\headfont
      \setlength\@lnumwidth{5.5\zw}%
      \advance\leftskip\@lnumwidth
      \hskip-\leftskip
      #1\nobreak\hfil\nobreak\hbox to\@pnumwidth{\hss#2}\par
      \penalty\@highpenalty
    \endgroup
  \fi}
\renewcommand*{\l@section}{%
  \@dottedtocline{1}{1.5\zw}{4.5\zw}}
\makeatother

% Page headers
\renewcommand{\chaptermark}[1]{\markboth{第\thechapter 章\quad #1}{}}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\nouppercase{\leftmark}}
\fancyhead[R]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}
\fancypagestyle{plain}{%
  \fancyhf{}%
  \fancyhead[L]{\nouppercase{\leftmark}}%
  \fancyhead[R]{\thepage}%
  \renewcommand{\headrulewidth}{0.4pt}%
  \renewcommand{\footrulewidth}{0pt}%
}
\fancypagestyle{plainhead}{%
  \fancyhf{}%
  \fancyhead[L]{\nouppercase{\leftmark}}%
  \fancyhead[R]{\thepage}%
  \renewcommand{\headrulewidth}{0.4pt}%
  \renewcommand{\footrulewidth}{0pt}%
}

% Japanese captions
\renewcommand{\figurename}{図}
\renewcommand{\tablename}{表}
\floatname{listing}{コード}
